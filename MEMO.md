# MZ-700 でゲームを作る忘備録
一部は個人の偏見が含まれます. 正しくないかもしれません<br/>
内容は予告なく更新されることがあります<br/>

目次
- [MZ-700 でゲームを作る忘備録](#mz-700-でゲームを作る忘備録)
  - [ハードウェア/エミュレータでの問題](#ハードウェアエミュレータでの問題)
      - [PCG-700 - 反応してしまう](#pcg-700---反応してしまう)
      - [EmuZ-700 - 音を止める方法](#emuz-700---音を止める方法)
      - [EmuZ-700 - ldi, ldir 命令等をデバッガで実行さない](#emuz-700---ldi-ldir-命令等をデバッガで実行さない)
      - [MZ-1500 - モニターによる RAM 破壊](#mz-1500---モニターによる-ram-破壊)
      - [MZ-1500 - I レジスタは使わない](#mz-1500---i-レジスタは使わない)
  - [開発言語](#開発言語)
      - [コンパイラは, z88dk の SDCC を使用します](#コンパイラは-z88dk-の-sdcc-を使用します)
      - [独自の命名規約](#独自の命名規約)
      - [なかなか覚えない C ポインタ](#なかなか覚えない-c-ポインタ)
      - [アセンブラ](#アセンブラ)
  - [ストレージ](#ストレージ)
      - [ストレージとなるデータやコードの選定](#ストレージとなるデータやコードの選定)
      - [ソース](#ソース)
      - [独自セクションとしてコンパイル](#独自セクションとしてコンパイル)
      - [圧縮アルゴリズムの選択 (独断と偏見)](#圧縮アルゴリズムの選択-独断と偏見)


## ハードウェア/エミュレータでの問題

#### PCG-700 - 反応してしまう
  - [**問題**] 0xd000-0xffff のバンクが RAM の状態であっても, PCG-700 は, 0xe010-0xe012 への書込に反応します
  - [**対策**] 0xe010-0xe012 にデータを書き込んだ後は, PCG-700 を無効化してください
  ```
    // バンクをVRAM側に切り替えた後
    ld      A, 0x08
    ld      (0xe012), A
  ```

#### EmuZ-700 - 音を止める方法
  - **[問題]** 実機では音程(CT0)に 0 を書き込むと音が止まりまが, EmuZ-700 では音が鳴ってしまいます
  - **[対策]** 8253 モード再セットによりカウンタを停止させましょう

#### EmuZ-700 - ldi, ldir 命令等をデバッガで実行さない
  - **[問題]** VRAM に対して ldi, ldir 等を行う際, デバッガで実行させると, デバッガが応答しなくなることがあります
  - **[対策]** 原因不明ですが, できるだけ上記の動作は行わないようにしましょう. 将来のバージョンアップで治るかもしれません

#### MZ-1500 - モニターによる RAM 破壊
  - **[問題]** 0xc000-0xc02f はモニターサブルーチンがワークとして使うので, リセットするとこの部分が破壊されます
  - **[対策]** 色々考えられますが...
    - コードやデータはここを避ける
    - この部分を BSS セクションにする
    - 起動前に 0xc000-0xc02f の内容をどこかに確保しておいて, 起動時にリストアする
    - 面倒なので対応しないで「MZ-1500ではリセットしないでください」と書く

#### MZ-1500 - I レジスタは使わない
  - **[問題]** I レジスタの値がそのままアドレスバスに流れており, I の値によっては余計なウエイトがかかったりします
  - **[対策]** I レジスタは使わないようにしましょう


## 開発言語

#### コンパイラは, z88dk の SDCC を使用します
  - いずれは, 単体の SDCC で書きたい
  - 呼び出し規約は, 出来る限り __z88dk_fastcall を使います
    - __sdcccall(1) のほうが効率が良いのですが, z88dk 版 SDCC はまだ対応していません<br/>
      (無理矢理使うと, 対応してない旨の警告が表示されます)
  - **[注意]** グローバル変数は全て関数の外に書いてください. 内側に書くと, 余計な初期化コードを生成してしまいます
  - **[注意]** static const 定数は関数の外に書いてください. 内側に書くと, BSS セクションに置かれることがあります (理由不明)
#### 独自の命名規約

  |           |                |
  |-----------|----------------|
  |ファイル名 |kebab-case      |
  |変数名     |snake_case      |
  |関数名     |canalCase       |
  |マクロ,定数|UPPER_SNAKE_CASE|
  |クラス     |PascalCase      |

 - 独自の変数名として, xxxx_align256 ～ xxxx_align256_end があります<br/>
   この間は 256バイト境界内にあるかどうかを, align256_checker.php を使ってチェックします
#### なかなか覚えない C ポインタ
  ```
  void const (* const func)(void)      // 関数へのポインタ
  void const (* const func[])(void)    // 関数へのポインタの配列
  T const *                            // T (アドレス)が const
  T * const                            // T の指すデータが const
  ```
#### アセンブラ
  - アセンブラは使用しないで, インライン アセンブラを使います
  - 書き方は SDCC old style「__asm～__endasm;」がお勧めです<br/>
    世の中は GCC 風の文字列が主流ですが, 文字列だと C のマクロが使えません
  - アセンブラ化は関数単位にしましょう. C コードと混ざると最適化ができません
  - 使用するセクションは, C と同じく, code_compiler, rodata_compiler, data_compiler, bss_compiler


## ストレージ
  ここでいう「ストレージ」は, 圧縮した複数のデータやコードを集合体で, 適時展開エリアに展開して使用します<br/>
  例えば, Baryon では, ストレージに シーン, ステージ, ボス等, 背景 の4種類のデータやコードが数十個格納されてます
#### ストレージとなるデータやコードの選定
  - ストレージ化に向いてるかを調べます<br/>
    ステージデータや背景のように, 複数あっても同時に1つしか存在できない(排他的)ものが向いてます
  - 展開先アドレスと展開後(圧縮前)の最大サイズを調査して決定します<br/>
    展開後が10バイトと10000バイトのデータの混在のストレージ化は, 無駄が多いかもしれません
#### ソース
  - データやコード1個につき1ソースファイルに制限しておくと管理しやすいです
  - 展開後のコードの呼び出し先アドレスは, 1つしかないのが普通ですので, 複数の機能を持たせたい場合は引数で実現するように設計してください
  - **[重要]** ソースは, エントリー関数を最初に書いてください. テーブルなどは後に書きます
#### 独自セクションとしてコンパイル
  ここでは, 0xb000 で動く code_xxxxx というセクション向けのコードを考えましょう
  - **[重要]** ソースの冒頭に, セクションを追加します. <br/>
    z88dk には code と const だけ独自のセクションにコンパイルする機能があります(何故か独自の bss セクションは指定できません)
    ```
    #pragma codeseg code_xxxxx
    #pragma constseg code_xxxxx
    ```
  - **[注意]** 空の関数を書くと, 通常の code セグメントに置かれてしまいます!
  - 独自セクションを用意したら, アセンブラ ソース(例えば crt0.asm) に code_xxxx のアドレスを確定させます (多分コンパイル オプションにもあると思うが調べてない)
    ```
    SECTION code_xxxxx
    org     0xb000
    ```
  - コンパイルすると 「foo_code_xxxxx.bin」 のようなセクション名のついたバイナリができます
  - **[注意]** バイトサイズが展開用エリアを超えないようにチェックしてください
#### 圧縮アルゴリズムの選択 (独断と偏見)
  これからも高性能なアルゴリズムが発表されるでしょうから, 適時調査しましょう

  |アルゴリズム名|圧縮率(ざっくり)|展開コード(bytes)|ワーク(bytes)|圧縮速度|展開速度|メモ|
  |--------------|----------------|-----------------|-------------|--------|--------|----|
  |LZe           |60-70%          |                 |             |速い    |激速    |    |
  |ZX7           |60-70%          |                 |             |速い    |        |    |
  |Exomizer3     |55-60%          |220              |156          |速い    |やや遅い|    |
  |ZX0           |55-60%          |68               |0            |激遅※1 |速い    |※1 Salvador という高速圧縮ツールがあります |
  |Shrinkler     |50-65%          |                 |             |遅い    |激遅    |    |
  |Upkr          |50-65%          |195              |320          |遅い※2 |遅い    |※2 プログレスバーの表示に時間がかかってるので,これを削除して再ビルドすると速くなります(要Rust)|
